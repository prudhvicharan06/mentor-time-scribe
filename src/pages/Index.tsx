
// Render the new MentorWorkflow component as the main content

import MentorWorkflow from "@/components/MentorWorkflow";

const Index = () => {
  return (
    <div className="min-h-screen w-full bg-background flex items-start justify-center">
      <MentorWorkflow />
    </div>
  );
};

export default Index;
